# TME5 : Conway's Game of Life


Depuis la ligne de commande :

```
javac -d bin -cp bin -sourcepath src '$(find src -name '*.java')'
```

Lancer avec un mode et un nombre de worker :

```
java -cp bin life.LifeGame multi 4
```

